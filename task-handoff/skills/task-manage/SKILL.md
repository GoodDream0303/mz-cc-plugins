---
name: task-manage
description: 管理接力任务——删除任务、改TaskId/重命名/改状态、归档已完成、清理示例行与体检 INDEX 一致性、精简瘦身 handoff。当用户要清理、删除、重命名、归档接力任务,整理 INDEX 总账,或精简/瘦身某个 handoff(清过期失效内容、折叠已完成)时使用。
---

# task-manage — 接力任务管理

维护数据根目录下的任务总账与任务文件夹。支持五类操作:
**删除 / 改TaskId重命名改状态 / 归档已完成 / 清理体检 / 精简 handoff**。

数据根:`${user_config.handoff_root}`,总账 `INDEX.md`。所有 md 一律 **UTF-8 无 BOM**。
文件系统操作(移动/重命名/删除文件夹)用 shell;改 INDEX/handoff 内容用 Edit。

## 派发

先用 Read 读 `${user_config.handoff_root}/INDEX.md` 了解现有任务。再解析用户意图:
- **已含明确操作意图 + 目标任务**(如"删除 X 任务""把 Y 改成已完成")→ 跳过提问,直接执行对应小节。
- **意图不明确** → **先展示全量任务清单,再提问**:

  **第一步——展示全量清单(必做)**:用纯文本把 INDEX 里**全部任务**列成简表给用户看
  (序号 + 任务名 + 状态 + 最近更新),不要省略、不要只列 4 个。这一步是为了让用户能把
  心里想处理的任务与清单对上号——否则若其目标任务不在后面 AskUserQuestion 仅展示的 4 个选项里,
  容易对不上。

  **第二步——AskUserQuestion 一次性问两道题(同屏)**,**不要自己猜**:

  1. **问题一「任务」**:从 INDEX 取**状态为 🔄进行中 / ⬜阻塞**、按"最近更新"倒序的**至多 4 个**任务作为选项
     (label 用简明任务名,description 附状态 + 最近更新)。其余任务靠系统自动追加的 **Other** 手输任务名。
  2. **问题二「操作」**:固定 **4 个**选项(5 类操作压成 4 项——把"归档已完成"与"清理示例/体检"合并为「整理 INDEX」):
     - **删除任务** → 走 §1
     - **改TaskId / 重命名 / 改状态** → 走 §2
     - **整理 INDEX(归档已完成 + 清理示例/体检)** → 走 §3 与/或 §4:运行时按 INDEX 现状判断——
       有 ✅已完成 任务则归档(§3),有 `<示例>` 行或磁盘/INDEX 不一致则清理体检(§4);两者都适用就都做。
     - **精简 handoff(瘦身进行中任务)** → 走 §5

  > 两题放进**同一个 AskUserQuestion 调用**(各 `multiSelect=false`)。
  > 若用户选的操作是「整理 INDEX」,该操作不针对单个任务——此时**忽略问题一的任务选择**,以问题二为准。

---

## 1. 删除任务(不可逆,务必谨慎)

1. 在 INDEX 定位该任务的文件夹。
2. **删除前先看目标**:用 Read 看一眼该 `handoff.md`,把任务名/状态/下一步摘要给用户,
   明确告知将**递归删除整个文件夹**(连同里面所有产物),请用户确认。
3. 用户确认后删除该文件夹(Windows PowerShell:`Remove-Item '<文件夹绝对路径>' -Recurse -Force`;
   类 Unix:`rm -rf '<文件夹绝对路径>'`)。
4. 用 Edit 从 `INDEX.md` 删掉对应那一行。
5. **重排序号**:把主表剩余各行的"序号"列重排成连续 `1..N`(示例行保持 0),消除删除留下的空缺。
6. 回显:已删除 `<文件夹>` 及 INDEX 中的行,并已重排序号。

> 若用户只想删 INDEX 行、保留文件夹(或反之),按其明确意思来,别擅自扩大删除范围。

## 2. 改TaskId / 重命名 / 改状态

可单项或组合。改完**三处都要同步**:文件夹名(仅重命名时)、`INDEX.md` 行、`handoff.md` 头部。

- **改 TaskId(任务单号)**:Edit 改 handoff 头部单号字段(`TaskId:`,旧文档或写作 `编号:`)与 INDEX 的 **TaskId 列**。
  > 注意:此处改的是**任务单号**,不是 INDEX 最左的"序号"列(序号=行号,删除/归档/体检时自动重排维护,一般不手改)。
- **改状态**:Edit 改 handoff 头部"状态"行与 INDEX 状态列(🔄/⬜/✅),并刷新"最近更新"。
- **重命名任务**(连带文件夹):
  1. 文件夹名规则仍是 `简明任务名_YYYYMMDD`,只换任务名部分、保留原日期。
  2. 重命名文件夹(PowerShell:`Rename-Item '<旧文件夹绝对路径>' '<新文件夹名>'`;类 Unix:`mv`)。
  3. Edit 改 INDEX 的"任务"列与"文件夹"列(`<新文件夹名>/handoff.md`)。
  4. Edit 改 handoff.md 标题行的任务名。
- 回显改了哪些字段、涉及哪些文件。

## 3. 归档已完成

把状态为 ✅已完成 的任务移出主表,保持 INDEX 清爽。

1. 确保 `${user_config.handoff_root}/archive/` 存在(无则新建目录)。
2. 把任务文件夹移入 `${user_config.handoff_root}/archive/`(PowerShell:`Move-Item`;类 Unix:`mv`)。
3. Edit `INDEX.md`:把该行从主表移到文件末尾的 `## 已归档` 区(无该标题则新建),
   文件夹列路径改为 `archive/<文件夹名>/handoff.md`,**该行"序号"列置为 `-`**(已归档不参与主表序号)。
4. **重排序号**:把主表剩余各行的"序号"列重排成连续 `1..N`(示例行保持 0)。
5. 可一次处理多个已完成任务;回显归档了哪些、已重排序号。

## 4. 清理示例行 / 体检一致性

1. **清理示例**:删掉 INDEX 里带 `<示例>` 字样的行。
2. **体检**:用 Glob 列出 `${user_config.handoff_root}` 下实际任务文件夹,与 INDEX 行比对,报告三类不一致:
   - **孤儿行**:INDEX 有、磁盘上找不到对应文件夹。
   - **漏登记**:磁盘上有任务文件夹、INDEX 里没有对应行。
   - **序号断裂**:主表"序号"列不是连续 `1..N`(删除/归档一般已即时重排,此处兜底)。
3. 只**报告**不一致并给修复建议(补行 / 删孤儿行 / 修路径 / 重排序号);**经用户确认后**再改,不擅自增删。

## 5. 精简 handoff(瘦身;先报告→确认→备份→应用,绝不误删决策与有效链接)

针对**仍在进行**的某个任务,清掉 handoff 里确证过期/失效/冗余的内容,把已完成细节折叠归档,
让正文回到"只看进行中与下一步"。与操作3区别:操作3挪走整个**已完成任务**;本操作是进行中任务的
**内部减负**,不改任务状态。与 **task-save 轻量维护**的区别:save 每会话随手只碰「本会话相关 + 明显矛盾」;
本操作是**用户显式发起的全量深度精简**(逐条验证失效、可回溯历史),范围更广、改动更大。

1. **定位并通读**:据用户给的任务名,在 INDEX 定位其 `handoff.md`,用 Read 通读全文。
2. **只读分类**(先不改任何东西),分三类:
   - **确证失效**(机械验证):区块"关键链接与路径"里的**本地路径**用 Test-Path 查是否仍存在;
     引用的已删除仓库/文件夹标失效。URL **不主动联网**,只按上下文判断是否被取代,标"疑似"。
   - **文档内可推断的过期**:"下一步"里已在"当前进度"标 ✅ 的条目;"未决问题"里正文别处已解决的;
     被后续决策取代的旧"关键决策";重复/冗余表述。
   - **完成项归档候选**:进度区块里 ✅已完成 的细节项。
3. **产出精简报告**:逐项 `[区块 | 原文摘要 | 处置建议(删/归档/合并/保留待定) | 理由 | 置信度]`。
   决策类、存疑链接默认**保留**并标"需你判断"。
4. **逐项/批量确认**:用户勾选采纳哪些;**未确认的绝不改动**。
5. **备份后应用**:先把原文件复制为同目录 `handoff.bak`(UTF-8);再用 Edit 应用已确认项:
   - 完成项 → 压成一句话摘要,移到 handoff 末尾 `## 已完成存档`(无则新建);
   - 确证失效链接 / 已解决未决 / 过期下一步 / 被取代决策 → 删除或标注;
   - 刷新 handoff 头部"最近更新"。
6. **同步 INDEX**:精简通常不改状态;仅"最近更新"可刷新。状态变化只在用户明确时才动。
7. **回显**:列出实际改了什么、备份在 `handoff.bak`,提示不满意可从 .bak 还原。

> 红线:**任务目标、仍有效的关键决策与权衡、仍有效的下一步与关键链接,一律保留**。
> 本操作只做"减"且每处删改都经用户点头;有疑问就保留,不替用户拿主意。
